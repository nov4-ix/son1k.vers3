# Son1k Frontend (Vite + React)

## Dev
```bash
npm install
npm run dev
# http://localhost:3000
```
Create `.env` from `.env.example` to point to your API.

## Build
```bash
npm run build
npm run preview
```
